# مواصفات نظام إدارة الشفتات والتسليم والاستلام

## التعديلات المطلوبة على صفحة إدارة الشفتات

### 1. إزالة الروابط الزائدة
- ✅ إزالة رابط "سجل الشفتات الكامل" لأنه موجود في الـ Sidebar

### 2. تصميم الجدول الجديد (3 أعمدة فقط)

#### الأعمدة:
1. **العهدة** - اسم المحفظة/الماكينة/الدرج
2. **الرصيد** - الرصيد الفعلي الحالي
3. **الإجراء** - أزرار التسليم والاستلام

#### ترتيب العناصر في الجدول:
1. **المحافظ الإلكترونية** (Wallets) - تحت بعض
2. **الماكينات** (Machines) - تحت المحافظ
3. **أدراج الكاش** (Drawers) - تحت الماكينات
4. **طلبات التحويل بين الموظفين** - لو موجودة

### 3. قواعد الاستلام والتسليم

#### قواعد الاستلام:
- لا يمكن لموظفين استلام نفس المحفظة في نفس الوقت
- عند الاستلام يجب كتابة **الرصيد الفعلي** في يد الموظف
- إذا كان هناك فرق بين الرصيد المتوقع والفعلي:
  - يجب كتابة **سبب الاختلاف**
  - النظام يقيّم الفرق تلقائياً:
    - **عجز بسيط (0-10)** في المحافظ = طبيعي (رسوم تحويلات)
    - **زيادة (20-30)** في الماكينات = طبيعية (عمولات شركات)
    - غير ذلك = يحتاج مراجعة ⚠️

#### قواعد التسليم:
- الموظف يمكنه تسليم أي محفظة في عهدته لزميل آخر
- يتم إرسال طلب تسليم للزميل المستقبل
- الزميل المستقبل يجب أن يؤكد الاستلام بإدخال الرصيد الفعلي

### 4. قواعد بدء وإنهاء الشفتات

#### بداية الشفت الصباحي (أول موظف):
- ❌ **ممنوع** تسجيل أي مبيعات قبل استلام:
  1. جميع المحافظ
  2. جميع الماكينات
  3. درج الكاش الخاص به
- ✅ بعد الاستلام الكامل → يفتح البيع

#### بداية شفت متداخل (موظف ثاني):
- يجب استلام **درج الكاش فقط**
- المحافظ والماكينات تعمل مع الزميل الأول
- ✅ بعد استلام الدرج → يفتح البيع له

#### إنهاء شفت لموظف (ليس الأخير):
- يجب تسليم **درج الكاش** الخاص به فقط
- لا يمكن تسجيل الخروج قبل التسليم

#### إنهاء شفت لآخر موظف (إغلاق اليوم):
- ⚠️ **النظام يرفض** إنهاء الشفت إذا حاول تسليم الدرج فقط
- **يجب** تسليم وتأكيد أرصدة:
  1. جميع المحافظ
  2. جميع الماكينات
  3. درج الكاش
- هذا لضمان دقة الأرصدة للشفت الصباحي التالي

### 5. تقرير التسليم والاستلام

#### معلومات يجب توثيقها:
- اسم الموظف المسلّم
- اسم الموظف المستلم
- اسم العهدة (محفظة/ماكينة/درج)
- الرصيد المتوقع
- الرصيد الفعلي المستلم
- الفارق (إن وجد)
- سبب الفارق
- تقييم النظام الآلي
- التاريخ والوقت
- ID الشفت المرتبط

#### المراجعة الآلية:
- يجب أن يوضح النظام **تلقائياً** سبب أي تغيير في الأرصدة:
  - "عجز طبيعي - رسوم تحويلات"
  - "زيادة طبيعية - عمولات شركات"
  - "يحتاج مراجعة إدارية"

### 6. العجز والزيادة المقبولة

#### في المحافظ:
- **عجز 0-10**: طبيعي ✅ (رسوم تحويلات)
- **أي عجز آخر**: يحتاج توضيح ⚠️

#### في الماكينات:
- **زيادة 20-30**: طبيعية ✅ (عمولات تلقائية من الشركات)
- **أي زيادة أخرى**: يحتاج توضيح ⚠️

### 7. حالة قفل المبيعات

#### متى تُقفل المبيعات:
- عند بدء شفت جديد قبل استلام العهدة
- عند نهاية الشفت قبل تسليم العهدة

#### رسالة القفل:
```
⚠️ المبيعات مقفولة حالياً:
يجب استلام جميع المحافظ والماكينات والدرج لفتح المبيعات
```

أو

```
⚠️ المبيعات مقفولة حالياً:
يجب استلام درج الكاش الخاص بك لفتح المبيعات
```

## الملفات المطلوب تعديلها:
1. `src/app/(dashboard)/shifts/page.tsx` - الواجهة
2. `src/app/api/custody/handover/route.ts` - API التسليم والاستلام
3. `src/app/api/shifts/route.ts` - API إدارة الشفتات

## قاعدة البيانات المطلوبة:
- جدول `custody_handovers` - سجل التسليم والاستلام
- حقول: id, wallet_id, from_user, to_user, expected_balance, actual_balance, difference, reason, audit_status, shift_id, timestamp
